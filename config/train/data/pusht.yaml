dataset:
  num_steps: ${eval:'${num_preds} + ${history_size}'}
  frameskip: 5
  name: pusht_expert_train.h5
  keys_to_load:
    - pixels
    - action
    - proprio
    - state
  keys_to_cache: []
  rdcc_nbytes: 268435456
  rdcc_w0: 1.0
